
# MAIN ----------------------------------------------------------------------------------------

/:
  controller: Main
  action: home

/a-propos:
  controller: Main
  action: aboutUs

/login:
  controller: Security
  action: login

/register:
  controller: Security
  action: register

/account:
  controller: Security
  action: modifieAccount

/dashboard:
  controller: Main
  action: dashboard

/contact:
  controller: Main
  action: contact


# SECURITY ------------------------------------------------------------------------------------

/logout:
  controller: Security
  action: logout

/forgot-password:
  controller: Security
  action: pwdForget

/verify2:
  controller: Security
  action: ChangePasswordVerification

/verify:
  controller: Security
  action: verifyEmail

/email-confirmed:
  controller: Security
  action: confirmedEmail

/needtologout:
  controller: Security
  action: needToLogout

/password-changed:
  controller: Security
  action: passwordChangedSucces

/delete-account:
  controller: Security
  action: DeleteAccount

/installer:
  controller: Installer
  action: installer


# COMMENTS ---------------------------------------------------------------------------------------

/dashboard/comments:
  controller: Comment
  action: showAllComments

/commentUpdate/{commentId}:
  controller: Comment
  action: commentUpdate

/commentDelete/{CommentId}:
  controller: Comment
  action: commentDelete


# PAGE ---------------------------------------------------------------------------------------

/dashboard/pages:
  controller: Page
  action: showAllPages

/dashboard/pages/createPage:
  controller: Page
  action: createPage

/dashboard/pages/editPage/{pageId}:
  controller: Page
  action: editPage

/dashboard/pages/deletePage/{pageId}:
  controller: Page
  action: deletePage

/dashboard/pages/confirmDeletePage/{pageId}:
  controller: Page
  action: confirmDeletePage

/page/{pageId}:
    controller: Page
    action: showPage



# ARTICLES ---------------------------------------------------------------------------------------

/dashboard/articles:
  controller: Article
  action: showAllArticles

/dashboard/articles/createArticle:
  controller: Article
  action: createArticle

/dashboard/articles/editArticle/{articleId}:
  controller: Article
  action: editArticle

/dashboard/articles/deleteArticle/{articleId}:
  controller: Article
  action: deleteArticle

/dashboard/articles/confirmDeleteArticle/{articleId}:
  controller: Article
  action: confirmDeleteArticle

/article/{articleId}:
    controller: Article
    action: showArticle


# USERS ---------------------------------------------------------------------------------------

/dashboard/users:
  controller: User
  action: showAllUsers

/dashboard/users/createUser:
  controller: User
  action: createUser

/dashboard/users/deleteUser/{userId}:
  controller: User
  action: deleteUser

/dashboard/users/confirmDeleteUser/{userId}:
  controller: User
  action: confirmDeleteUser

/dashboard/users/editUser/{userId}:
  controller: User
  action: editUser





