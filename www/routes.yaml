/:
  controller: Main
  action: home

/a-propos:
  controller: Main 
  action: aboutUs

/login:
  controller: Security
  action: login

/logout:
  controller: Security
  action: logout

/register:
  controller: Security
  action: register

/forgot-password:
  controller: Security
  action: pwdForget

/account:
  controller: Security
  action: modifieAccount

/verify:
  controller: Security
  action: verifyEmail

/email-confirmed:
  controller: Security
  action: confirmedEmail


